{% extends 'base.jinja2' %}
{% block content %}
    <h2>{{user.username}}</h2>
    <div>{{user.email}}</div>
    <div>{{user.common_name}}</div>
    <div>{{user.created_at}}</div>
    {{form|safe}}
{% endblock content %}
